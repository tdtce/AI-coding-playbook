{{- $pages := slice -}}
{{- range .Site.RegularPages -}}
  {{- $pages = $pages | append (dict
    "title" .Title
    "url" .RelPermalink
    "section" (.CurrentSection.Title | default .Section)
    "content" .Plain
  ) -}}
{{- end -}}
{{- $pages | jsonify -}}
